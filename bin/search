#!/bin/zsh

W="$1"

if [ -z "$W" ]
then
	echo 'please specify argument'
	exit 2
fi

shift
if [ "$1" = "-0" ]
then
    shift
    find . -iname "*$W*" "$@" -print0
else
    find . -iname "*$W*" "$@" | grep --color=auto -i "$W"
fi
