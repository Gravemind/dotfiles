# https://wiki.archlinux.org/index.php/Steam/Troubleshooting#Text_is_corrupt_or_missing

ctl.dmixer {
  type pulse
}

pcm.pulse {
  type pulse
}

ctl.pulse {
  type pulse
}

pcm.!default {
  type pulse
}

ctl.!default {
  type pulse
}
