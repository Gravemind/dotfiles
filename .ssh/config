Host *
    Protocol 2
    ForwardAgent no
    ForwardX11 no
    #PasswordAuthentication no

    # force use IdentityFile
    IdentitiesOnly yes
    # adds keys to agent with ssh-add -c (-c: ask before use)
    AddKeysToAgent yes

    #AddressFamily inet

    # Faster tramp
    #   http://3gfp.com/wp/2014/08/emacs-tramp-and-speedier-ssh-access/
    #   https://www.linux.com/news/accelerating-openssh-connections-controlmaster
    # But exposes agent to everyone !?
    # ControlMaster autoask
    # ControlPath ~/.ssh/master-%r@%h:%p

Include ~/.ssh/config.local

#Host ant.host.com
#    HostName real.host.name.com
#    IdentityFile ~/.ssh/id_rsa
