#
# Note: ssh config is "highest precedence first": the first parameter value seen
# is used.
#

# Local config
Include ~/.ssh/config.local

#Host custom.host.com
#    HostName real.host.com
#    Port 6666
#    User user
#    IdentityFile ~/.ssh/id_rsa
#    ControlPath ~/.ssh/cm-%C
#    AddressFamily inet # ipv4

# # Create per-account fake ssh URLs with per-account ssh key
# # See .gitconfig pushInsteadOf to use those URLs automatically
# # $ ssh-keygen -t ed25519 -C "github ACCOUNT" -f ~/.ssh/id_gh_ACCOUNT
# Host github.com
#     IdentityFile /NO_DEFAULT_IDENTITYFILE__CHOOSE_FAKE_URL_FROM_SSH_CONFIG
# Host github.com-ACCOUNT
#     Hostname github.com
#     IdentityFile ~/.ssh/id_gh_ACCOUNT

# Fallback/Default config (must be at the end)
Host *
    Protocol 2
    ForwardAgent no
    ForwardX11 no
    #PasswordAuthentication no

    # Force use of IdentityFile
    IdentitiesOnly yes
    # Auto-add keys to ssh agent
    AddKeysToAgent yes

    #AddressFamily inet

    # Faster connections, faster emacs tramp
    #   http://3gfp.com/wp/2014/08/emacs-tramp-and-speedier-ssh-access/
    #   https://www.linux.com/news/accelerating-openssh-connections-controlmaster
    #   ( https://hackertarget.com/ssh-examples-tunnels/ )
    ControlMaster auto
    ControlPersist 5m
    # ControlPath ~/.ssh/control-%C
